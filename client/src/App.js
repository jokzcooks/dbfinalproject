import './App.css';
import {
  HashRouter as Router,
} from "react-router-dom";
import Base from './containers/Base';
const Routes = () => {
  return (
    <Router>
      <Base />
    </Router>
  );
};

export default Routes;
